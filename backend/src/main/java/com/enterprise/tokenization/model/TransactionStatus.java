package com.enterprise.tokenization.model;

/**
 * Enumeration representing the status of a blockchain transaction.
 */
public enum TransactionStatus {
    PENDING,
    SUCCESS,
    FAILED,
    CONFIRMED
}